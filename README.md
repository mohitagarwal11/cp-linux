So making a cp command for linux in C.

basic feature: it allows users to copy a file frm source destination to final destination

so i need source and final destination args
check if both exist or not
and do the needful

WHAT I WANT MORE:
-keep the file premissions and type intact
-make it more efficient than the actual cp command
-if source doesnt exist error but if destination doesnt exist then create one maybe
-i also want to allow writes or file conversion
like from .c to txt or smt lets see
